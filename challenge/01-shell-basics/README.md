# Challenge 1: Shell Basics

**Description:** Navigate and manipulate the filesystem using shell commands.

## Brief
A script fails because of PATH and permissions. Fix it and print the flag.

## Tasks
1. Inspect `bin/hello` and `scripts/run.sh`.
2. `run.sh` __should__ find `hello` via PATH and execute it.
3. Fix file permissions and PATH export.